{% block image_widget %}
    {% spaceless %}
        {% if image_path %}
            <img src="{{ image_path | imagine_filter(filter) }}" class="{{ class }}" style="margin:0;" />
         {% endif %}
    {% endspaceless %}
{% endblock %}